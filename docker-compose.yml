services:

  app:
    build: .
    ports:
      - "8001:8001"
    depends_on:
      - ollama
      - chromadb

  ollama:
    image: ollama/ollama
    ports:
      - "11434:11434"

  chromadb:
    image: chromadb/chroma
    ports:
      - "8000:8000"